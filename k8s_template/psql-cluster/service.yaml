apiVersion: v1
kind: Service
metadata:
  name: psql-cluster
  namespace: psql-cluster
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  selector:
    postgresql.cnpg.io/cluster: psql-cluster